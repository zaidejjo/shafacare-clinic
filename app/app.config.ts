// https://ui.nuxt.com/docs/getting-started/theming
export default defineAppConfig({
  ui: {
    /**
     * Primary color: Uses the custom 'primary' palette defined
     * in our Tailwind CSS @theme block (deep burgundy / crimson).
     * Neutral: 'slate' provides clean, clinical undertones.
     */
    colors: {
      primary: 'primary',
      neutral: 'slate'
    }
  }
})
